# cloud

**Closed source — not in this repo.** Auth, projects, API keys, dashboard, device fleet management, licensing, telemetry, team organizations, managed updates, billing, and enterprise sync — the Cloud Platform layer described in the root README's architecture section.

This folder is a structural placeholder only, so the layered architecture is visible from the monorepo root. The actual implementation lives in [`portixhq/portix-cloud`](https://github.com/portixhq/portix-cloud) (private), not here — **do not add proprietary code to this folder.**

Roadmap: month 3-4, once the runtime moves beyond auto-generated local API keys.
