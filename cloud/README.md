# cloud

**Planned — not started.** Authentication, projects, API keys, analytics, licensing, and dashboard (Cloud Platform layer). Backed by Supabase (Postgres + Edge Functions), consistent with the rest of the stack — no transitional tooling. Roadmap: month 3-4, once the runtime moves beyond auto-generated local API keys.
