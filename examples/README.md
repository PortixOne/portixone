# Examples

- [`quickstart-html/`](quickstart-html) — minimal HTML page that uses `@portix/sdk` to print a test ticket against the local runtime. Serves the "Time to First Print" goal directly.

Requires `npm run build` (in the `sdk-js` workspace) before opening `index.html`, and the runtime running (`npm run dev`).
