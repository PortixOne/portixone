# cli

**Planned — not started.** Command-line interface for PortixOne — install/manage the runtime, register printers, and send test print jobs without writing code. Open source (MIT), same license as the runtime and SDKs.
